data:
  source: ../nnue_data
  pattern: "worker_*/chunk_*.txt"
  val_workers: [30, 31]
  feature_kind: half_ka_v2_hm

model:
  n_features: 16536
  l1: 512
  l2: 32
  l3: 32
  feature_kind: half_ka_v2_hm

train:
  batch_size: 2048
  max_epochs: 384
  early_stopping_patience: 32
  lr: 0.001
  weight_decay: 0.0
  num_workers: 4
  prefetch_factor: 2
  device: cpu
  checkpoint_dir: checkpoints
  log_every: 50
